<Window x:Class="EquipmentAccounting.Oborudovanie"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EquipmentAccounting"
        mc:Ignorable="d"
        Title="Oborudovanie" Loaded="Window_Loaded" Height="450" Width="800">
    <Grid>
        <DataGrid Name="dgogrenci" HorizontalAlignment="Left" AutoGenerateColumns="False"  VerticalAlignment="Top">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id_equipment}" Header="Номер оборудования" Width="3*"/>
                <DataGridTextColumn Binding="{Binding Name_equipment}" Header="Название" Width="3*"/>
                <DataGridTextColumn Binding="{Binding Id_type}" Header="Номер типа" Width="3*" />
                <DataGridTextColumn Binding="{Binding Id_plot}" Header="Номер участка" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</Window>
